apiVersion: v1
kind: ConfigMap
metadata:
  name: guestbook-config
  namespace: guestbook
  labels:
    app: guestbook
    project: guestbook
data:
  DB_HOST: guestbook-mariadb
  DB_PORT: "3306"
  DB_NAME: guestbook
